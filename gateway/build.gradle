dependencies {
    compile project(':common')

    compile "io.projectreactor.addons:reactor-extra"

    testCompile "junit:junit:$junit_version"
}

publishing {
    publications {
        gateway(MavenPublication) {
            artifactId 'Discord4J-gateway'

            from components.java

            artifact sourcesJar
            artifact javadocJar
        }
    }
}
